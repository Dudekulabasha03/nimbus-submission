# ── AI Provider Configuration ──────────────────────────────────────────────
# 2. Anthropic Claude (fallback — leave blank if not available)
ANTHROPIC_API_KEY=
# Ollama Local Model Configuration
# Set USE_OLLAMA=true to bypass Anthropic and use a local model via Ollama
USE_OLLAMA=false
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
