version: "3.9"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist SQLite DB across restarts
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-info}
    restart: unless-stopped
